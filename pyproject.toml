[project]
name = "agentic-footprint-tests"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
test = [
  "pytest==9.1.1",
]

[tool.uv]
package = false

[tool.agentic-footprint.dependency-policy]
# Dependency automation must wait at least seven days after a package release.
minimum-release-age-days = 7
