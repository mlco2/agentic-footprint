"""The `af_estimator` sidecar: wraps `ecologits` to estimate the
environmental impact of remote LLM calls.

Run directly as a script (`python __main__.py`), not via `-m`;
`af-sidecar::Sidecar::spawn` invokes sidecars by script path in this PoC.
"""
